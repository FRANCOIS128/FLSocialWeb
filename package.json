{
  "name": "flsocial",
  "private": true,
  "version": "1.0.0",
  "description": "FLsocial - social web app with login/register auth",
  "scripts": {
    "dev:frontend": "pnpm --dir frontend dev",
    "dev:backend": "pnpm --dir backend dev",
    "build:frontend": "pnpm --dir frontend build",
    "install:all": "pnpm --dir frontend install && pnpm --dir backend install",
    "db:push": "pnpm --dir backend exec prisma db push",
    "db:seed": "pnpm --dir backend run seed",
    "db:setup": "pnpm --dir backend exec prisma db push && pnpm --dir backend run seed"
  },
  "packageManager": "pnpm@10.17.1"
}
